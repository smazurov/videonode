videonode
.vscode/
# .claude/ session + runtime state is ignored; skills/ is committed.
# Listing children explicitly because git can't re-include a path once
# its parent is ignored.
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
vendor/
tmp/
.env
build/
bin/
!composer/src/bin/

streams.toml
dist/
data/
composer/build/
composer/compile_commands.json
.claude/skills/playwright-cli/
.playwright*/

# Recording sessions (fMP4/HLS + storyboards) written by the daemon
recordings/
