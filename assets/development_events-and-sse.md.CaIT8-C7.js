import{c as o,$ as r,Q as a,j as d,m as l,h as c,ax as n,n as e,o as h,S as p,g as s}from"./chunks/framework.DGHXLY8X.js";const g=JSON.parse('{"title":"Events and SSE","description":"","frontmatter":{},"headers":[],"relativePath":"development/events-and-sse.md","filePath":"development/events-and-sse.md","lastUpdated":1780186924000}'),u={name:"development/events-and-sse.md"};function b(v,t,m,k,E,y){const i=r("Mermaid");return a(),d("div",null,[t[1]||(t[1]=l(`<h1 id="events-and-sse" tabindex="-1">Events and SSE <a class="header-anchor" href="#events-and-sse" aria-label="Permalink to &quot;Events and SSE&quot;">​</a></h1><p>This page explains how VideoNode delivers real-time UI updates: the in-process event bus, the two Server-Sent Events (SSE) streams, and the uniform event envelope. For the REST surface that SSE complements, see <a href="./../reference/rest-api">the REST API</a>.</p><h2 id="the-event-bus" tabindex="-1">The event bus <a class="header-anchor" href="#the-event-bus" aria-label="Permalink to &quot;The event bus&quot;">​</a></h2><p>The daemon runs one in-process publish/subscribe bus (<code>internal/events</code>, wrapping <code>kelindar/event</code>). Producers call <code>events.Publish(bus, ev)</code>; consumers call <code>events.Subscribe(bus, fn)</code>. The concrete Go type selects the delivery topic, so adding an event type needs no change to the bus itself.</p><p>The bus is the hub, not the wire. SSE is one subscriber that bridges bus events to the browser. Other subscribers stay in-process: the device detector reacts to <code>StreamCrashedEvent</code> to recheck the HDMI signal and never touches SSE. Keeping the bus separate from SSE lets in-process reactions and browser pushes evolve independently.</p><h2 id="the-two-sse-streams" tabindex="-1">The two SSE streams <a class="header-anchor" href="#the-two-sse-streams" aria-label="Permalink to &quot;The two SSE streams&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Endpoint</th><th>Carries</th><th>Replay</th></tr></thead><tbody><tr><td><code>/api/events</code></td><td>entity envelope, global events, heartbeat</td><td>none</td></tr><tr><td><code>/api/logs/stream</code></td><td>structured log records</td><td>ring buffer, then live</td></tr></tbody></table><p><code>/api/events</code> is the multiplexed real-time feed. <code>/api/logs/stream</code> is separate because logs need backfill (the ring buffer) and a larger client buffer.</p><h2 id="the-entity-envelope" tabindex="-1">The entity envelope <a class="header-anchor" href="#the-entity-envelope" aria-label="Permalink to &quot;The entity envelope&quot;">​</a></h2><p>Every per-entity update rides one wire schema:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;entity_type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;source&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cam-lobby&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;action&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;payload&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {},</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;timestamp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-05-29T10:30:00Z&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>entity_type</code> is <code>source</code>, <code>composer</code>, or <code>stream</code>. <code>action</code> is one of:</p><ul><li><code>created</code>, <code>updated</code>, <code>deleted</code>: lifecycle. Payload is the full entity snapshot, or absent on delete.</li><li><code>status</code>: a runtime health snapshot from a sidecar.</li><li><code>metrics</code>: fps, dropped frames, bitrate.</li><li><code>consumers</code>: the per-client reader set.</li></ul><p>The UI discriminates on <code>(entity_type, action)</code> in one dispatcher (<code>ui/src/hooks/entityDispatch.ts</code>), so a new entity costs one map entry rather than a new event handler. Lifecycle events trigger a dependency fan-out: when a stream&#39;s upstream changes, <code>Registry.Touch</code> re-loads and republishes the affected source so its denormalized <code>consumers</code> rollup stays correct.</p><h2 id="global-events" tabindex="-1">Global events <a class="header-anchor" href="#global-events" aria-label="Permalink to &quot;Global events&quot;">​</a></h2><p>Three events sit outside the envelope because they are not per-entity:</p><ul><li><code>device-discovery</code>: a V4L2 device was added, removed, or changed. The UI refetches the device list.</li><li><code>pipeline-state-changed</code>: the daemon master switch toggled.</li><li><code>heartbeat</code>: emitted every 15 seconds to keep proxies and idle clients alive.</li></ul><h2 id="delivery-and-backpressure" tabindex="-1">Delivery and backpressure <a class="header-anchor" href="#delivery-and-backpressure" aria-label="Permalink to &quot;Delivery and backpressure&quot;">​</a></h2><p>Each SSE client gets a bounded channel. If a client falls behind, the bus drops events for it rather than blocking the publisher, so no slow reader can stall the daemon. SSE is therefore best-effort: the client treats a reconnect as a resync point and refetches over REST (the connection-status handler refetches streams on the transition back to online).</p><h2 id="what-is-live-versus-polled" tabindex="-1">What is live versus polled <a class="header-anchor" href="#what-is-live-versus-polled" aria-label="Permalink to &quot;What is live versus polled&quot;">​</a></h2><p>Different data uses different freshness mechanisms, one source of truth per datum:</p><table tabindex="0"><thead><tr><th>Data</th><th>Mechanism</th></tr></thead><tbody><tr><td>Stream, source, composer lifecycle / status / metrics / consumers</td><td>entity envelope on <code>/api/events</code></td></tr><tr><td>Device list</td><td><code>device-discovery</code> event, plus a 30s fallback poll</td></tr><tr><td>Pipeline switch</td><td><code>pipeline-state-changed</code> event</td></tr><tr><td>Live preview</td><td>MJPEG stream (<code>/api/sources/{id}/preview.mjpg</code>)</td></tr><tr><td>Daemon CPU / memory, process list</td><td>REST poll every 2s (<code>/api/system</code>, <code>/api/processes</code>)</td></tr><tr><td>Encoder capabilities, health</td><td>one-shot REST on load (boot-static)</td></tr></tbody></table><p>Per-entity runtime data rides the envelope, whole-daemon stats are polled, and boot-static capabilities are fetched once. The composer sidecar does not push status, so the UI shows composer process state from lifecycle events but no live canvas or consumer telemetry.</p><h2 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to &quot;Authentication&quot;">​</a></h2><p><code>EventSource</code> cannot set request headers, so the client passes credentials as a query parameter: <code>/api/events?auth=&lt;base64(user:pass)&gt;</code>. Serve the daemon over HTTPS so the query string is encrypted in transit.</p><h2 id="frontend-flow" tabindex="-1">Frontend flow <a class="header-anchor" href="#frontend-flow" aria-label="Permalink to &quot;Frontend flow&quot;">​</a></h2>`,26)),(a(),c(p,null,{default:n(()=>[h(i,{id:"mermaid-205",class:"mermaid",graph:"flowchart%20LR%0A%20%20accTitle%3A%20VideoNode%20event%20flow%20from%20producer%20to%20UI%0A%20%20accDescr%20%7B%0A%20%20%20%20A%20producer%20publishes%20to%20the%20in-process%20bus.%20The%20SSE%20handler%20is%20one%20bus%0A%20%20%20%20subscriber%20that%20forwards%20events%20over%20%2Fapi%2Fevents%20to%20the%20browser%20SSE%0A%20%20%20%20client%2C%20which%20dispatches%20entity%20events%20into%20Zustand%20stores.%20The%20device%0A%20%20%20%20detector%20is%20a%20second%20bus%20subscriber%20that%20reacts%20in-process.%0A%20%20%7D%0A%20%20prod%5B%22API%20%2F%20sidecar%22%5D%20--%3E%20bus%5B%22event%20bus%22%5D%0A%20%20bus%20--%3E%20sse%5B%22%2Fapi%2Fevents%20handler%22%5D%0A%20%20bus%20--%3E%20det%5B%22device%20detector%22%5D%0A%20%20sse%20--%3E%20client%5B%22SSEClient%20(browser)%22%5D%0A%20%20client%20--%3E%20disp%5B%22entityDispatch%22%5D%0A%20%20disp%20--%3E%20store%5B%22Zustand%20stores%22%5D%0A"})]),fallback:n(()=>[...t[0]||(t[0]=[e(" Loading... ",-1)])]),_:1})),t[2]||(t[2]=s("p",null,[e("The browser keeps one "),s("code",null,"SSEClient"),e(" ("),s("code",null,"ui/src/lib/api.ts"),e("), shared by every component through "),s("code",null,"useSSEManager"),e(". On error it reconnects with exponential backoff from 5s to 60s. "),s("code",null,"entityDispatch"),e(" routes each envelope into the matching store's "),s("code",null,"applyEntityEvent"),e(".")],-1))])}const S=o(u,[["render",b]]);export{g as __pageData,S as default};
