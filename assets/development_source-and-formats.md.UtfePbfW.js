import{c as o,Q as a,j as t,m as r}from"./chunks/framework.DGHXLY8X.js";const m=JSON.parse('{"title":"Source inputs and formats","description":"","frontmatter":{},"headers":[],"relativePath":"development/source-and-formats.md","filePath":"development/source-and-formats.md","lastUpdated":1780540607000}'),s={name:"development/source-and-formats.md"};function d(n,e,c,i,h,l){return a(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="source-inputs-and-formats" tabindex="-1">Source inputs and formats <a class="header-anchor" href="#source-inputs-and-formats" aria-label="Permalink to &quot;Source inputs and formats&quot;">​</a></h1><p><code>videonode-source</code> captures from one V4L2 device and broadcasts NV12 dma-bufs. This page explains the inputs it accepts, how it detects the pixel format, and why every input is normalized to NV12 before broadcast. For the process model and zero-copy transport, see <a href="./architecture">architecture</a>.</p><h2 id="the-nv12-only-wire-contract" tabindex="-1">The NV12-only wire contract <a class="header-anchor" href="#the-nv12-only-wire-contract" aria-label="Permalink to &quot;The NV12-only wire contract&quot;">​</a></h2><p>Every consumer (<code>videonode-sink</code>, <code>videonode-composer</code>, and the snapshot endpoint) receives NV12 and nothing else, because a single semi-planar format keeps consumers trivial: one Y plane, one interleaved UV plane, a fixed header shape. <code>videonode-source</code> therefore converts whatever the device produces into NV12 before it reaches the SCM_RIGHTS broadcast.</p><h2 id="input-classes" tabindex="-1">Input classes <a class="header-anchor" href="#input-classes" aria-label="Permalink to &quot;Input classes&quot;">​</a></h2><p>Two device classes reach the capture loop:</p><ul><li><strong>HDMI-RX bridge</strong> (for example <code>rk_hdmirx</code>). Exposes raw frames and supports DV timings plus <code>V4L2_EVENT_SOURCE_CHANGE</code>, so resolution and signal changes arrive in-band. Cable and signal truth come from <code>VIDIOC_QUERY_DV_TIMINGS</code>, not <code>V4L2_CID_DV_RX_POWER_PRESENT</code> (which reports present even with the cable unplugged).</li><li><strong>UVC</strong> webcams and HDMI-to-USB dongles. Deliver raw frames (YUYV and friends) or MJPEG. They reject the <code>SOURCE_CHANGE</code> subscribe by design, and their resolution is fixed for the streaming session, so a change goes through a full device reopen.</li></ul><p>A test-pattern mode (<code>test_mode</code>) bypasses capture entirely and feeds a synthetic frame for bring-up.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>HDMI input and MJPEG never coincide. HDMI bridges deliver raw frames (the RGA path); MJPEG comes only from UVC devices. That is why a <code>SOURCE_CHANGE</code> event never reaches the MJPEG decode path.</p></div><h2 id="format-detection" tabindex="-1">Format detection <a class="header-anchor" href="#format-detection" aria-label="Permalink to &quot;Format detection&quot;">​</a></h2><p>At open, <code>negotiate_format_</code> reads <code>VIDIOC_G_FMT</code> and picks a decode mode:</p><ul><li><code>pixel_format == MJPEG</code> selects <code>DecodeMode::Mjpeg</code>.</li><li>Anything else selects <code>DecodeMode::Rga</code>.</li></ul><h2 id="color-matrix" tabindex="-1">Color matrix <a class="header-anchor" href="#color-matrix" aria-label="Permalink to &quot;Color matrix&quot;">​</a></h2><p>Both decode modes trust the matrix V4L2 reports through <code>VIDIOC_G_FMT</code>, because the UVC color-matching descriptor and the <code>rk_hdmirx</code> colorspace are more reliable than a per-format assumption. <code>resolve_matrix</code> prefers an explicit <code>ycbcr_enc</code>, then the <code>colorspace</code> field, and falls back to the SD/HD convention (height &gt;= 720 maps to BT.709) only when the driver reports <code>DEFAULT</code>. MJPEG used to be hardcoded BT.601, which mislabeled HD webcams that are really BT.709. The opposite case also occurs: a camera that reports explicit ITU-R 601 even at 1080p resolves to BT.601, where the old hardcode happened to agree.</p><p>Hardware JPEG decoders can also report the JPEG&#39;s own matrix coefficients; MPP exposes this through <code>mpp_frame_get_colorspace</code>. When that signal is definite and disagrees with the V4L2 choice, the source adopts it once and logs the change. JFIF rarely carries it, so the V4L2 value usually stands. The software decode path exposes no matrix tag, so it always keeps the V4L2 value. The resolved matrix rides the broadcast header to the composer and the gRPC status to the daemon, where it sets the encoder&#39;s color flags and the read-only <code>color_matrix</code> field on <code>/api/sources</code>.</p><p>The pipeline models the matrix but not the quantization range. Every frame is tagged limited range on the wire and in the encoder flags. JPEG is conventionally full range, and V4L2 confirms it through the <code>Quantization</code> field, but no consumer reads the range today, so the source discards it. Carrying full range would need matching work in the composer shader and the ffmpeg flags.</p><h2 id="decode-and-conversion" tabindex="-1">Decode and conversion <a class="header-anchor" href="#decode-and-conversion" aria-label="Permalink to &quot;Decode and conversion&quot;">​</a></h2><p>videonode runs on any Linux host with a GPU. It uses hardware blocks where the platform provides them (Rockchip&#39;s MPP and RGA today) and portable software backends (<a href="https://libplacebo.org/" target="_blank" rel="noreferrer">libplacebo</a>, <a href="https://libjpeg-turbo.org/" target="_blank" rel="noreferrer">TurboJPEG</a>) everywhere else.</p><p>Raw frames take the <code>DecodeMode::Rga</code> path: non-NV12 input is color-converted to NV12 by the color-space-conversion (CSC) backend, which is Rockchip&#39;s <a href="https://github.com/tsukumijima/librga-rockchip" target="_blank" rel="noreferrer">RGA 2D accelerator</a> where a hardware accelerator is available, and a libplacebo software backend otherwise.</p><p>MJPEG frames take the <code>DecodeMode::Mjpeg</code> path and are decoded by one of two backends:</p><ul><li><strong><a href="https://github.com/tsukumijima/mpp-rockchip" target="_blank" rel="noreferrer">MPP</a></strong> (hardware decode) passes the JPEG&#39;s native chroma subsampling straight through, so a 4:2:0 source decodes to NV12, 4:2:2 to NV16, and 4:4:4 to NV24.</li><li><strong><a href="https://libjpeg-turbo.org/" target="_blank" rel="noreferrer">TurboJPEG</a></strong> (portable software, or when hardware decode init fails) always downconverts to NV12.</li></ul><p>Because MPP passes subsampling through, non-4:2:0 sources arrive as NV16 or NV24 and need a normalizing pass.</p><h2 id="subsampling-normalization" tabindex="-1">Subsampling normalization <a class="header-anchor" href="#subsampling-normalization" aria-label="Permalink to &quot;Subsampling normalization&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Decoded format</th><th>Path</th><th>Output</th><th>Notes</th></tr></thead><tbody><tr><td>NV12 (4:2:0)</td><td>passthrough</td><td>NV12</td><td>broadcast straight from the MPP pool, zero-copy</td></tr><tr><td>NV16 (4:2:2)</td><td>RGA CSC</td><td>NV12</td><td>the MACROSILICON VC-002 dongle; the tested path</td></tr><tr><td>NV24 (4:4:4)</td><td>RGA CSC</td><td>NV12</td><td>best-effort chroma; emits a one-shot warning</td></tr><tr><td>unknown</td><td>rejected</td><td>none</td><td>frame dropped, error logged</td></tr></tbody></table><p>The CSC ring is allocated on the first non-NV12 MJPEG frame, so 4:2:0 cameras stay zero-copy and pay nothing. When conversion cannot run (the backend lacks the subsampling, or the ring fails to allocate), the frame is dropped and the cause is logged once. The source goes black with a log line rather than stalling silently, and nothing non-NV12 ever reaches the wire.</p><h2 id="handoff" tabindex="-1">Handoff <a class="header-anchor" href="#handoff" aria-label="Permalink to &quot;Handoff&quot;">​</a></h2><p>The resulting NV12 dma-buf broadcasts to consumers over SCM_RIGHTS. See <a href="./architecture">architecture</a> for the wire format and <a href="./../reference/pipeline-model">pipeline model</a> for source, composer, and stream lifecycles.</p>',27)])])}const u=o(s,[["render",d]]);export{m as __pageData,u as default};
